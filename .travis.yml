version: ~> 1.0
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - libstdc++-4.9-dev
notifications:
  email: false
import:
  - source: form8ion/.travi-ci:node.yml
  - source: travi/.travis-ci:authenticated-semantic-release.yml
after_success: npm run coverage:report
